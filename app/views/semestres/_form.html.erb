<%= form_with(model: semestre, local: true) do |form| %>
  <% if semestre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(semestre.errors.count, "error") %> prohibited this semestre from being saved:</h2>

      <ul>
      <% semestre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ano %>
    <%= form.number_field :ano, id: :semestre_ano %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.number_field :tipo, id: :semestre_tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
