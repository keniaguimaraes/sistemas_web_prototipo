<p id="notice"><%= notice %></p>

<h1>Nota</h1>

<table>
  <thead>
    <tr>
      <th>Autor</th>
      <th>Semestre</th>
      <th>Disciplina</th>
      <th>Data</th>
      <th>Texto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @nota.each do |notum| %>
      <tr>
        <td><%= notum.autor %></td>
        <td><%= notum.semestre_id %></td>
        <td><%= notum.disciplina_id %></td>
        <td><%= notum.data %></td>
        <td><%= notum.texto %></td>
        <td><%= link_to 'Show', notum %></td>
        <td><%= link_to 'Edit', edit_notum_path(notum) %></td>
        <td><%= link_to 'Destroy', notum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Notum', new_notum_path %>
